import * as rls from "readline-sync";

// let base: número = readlineSync.questionInt("Ingrese la base: ");
// let exponente: number = readlineSync.questionInt("Ingrese el exponente: ");


// función calcular(base: número, exponente: número){
// si (exponente >= 0) {

// si (exponente === 0) {
// devuelve 1;
// } demás {
// return calcularPotencia(base,exponente);
// }
// }
// }

// función calcularPotencia(base: número, exponente: número):número {
// deja resultado: numero = 1;
// for (sea i: número = 0; i < exponente; i++) {
// resultado *= base;
// }
// devuelve resultado;
// }

// let potencia : any = calcular(base,exponente);

// console.log( base + " a la " + exponente + " potencia = " + potencia );


// //---------------------------------------------- ----------------------------------

// //Es múltiple
/*let numero : number = readlineSync.questionInt("Ingrese un número entero: ");
let numeroDos: number = readlineSync.questionInt("Ingrese otro numero para comprobar si es multiplo del anterior: ");

función esMultiplo(núm: número, multiploDe: número): booleano {
   let calcular: boolean = num % multiploDe === 0;
    calcular retorno;
}
let multiplo :boolean = esMultiplo(numero,numeroDos);
console.log(multiplo);*/


// //---------------------------------------------- --------------
// //Cantidad de divisores

// funcion cantidadDeDivisores(numero: numero): numero {
// let cantidad = 0;

// for (sea i = 1; i <= numero; i++) {
// if (esMultiplo(numero, i)) {
//cantidad++;
// }
// }

// devuelve cantidad;
// }
// console.log(cantidadDeDivisores(10));

// //---------------------------------------------- --------------
// función imprimirNombre(nombre: string, apellido: string): string {
// const nombreCompleto = `${nombre} ${apellido}`;
// console.log(nombreCompleto);
// return nombreCompleto;
// }